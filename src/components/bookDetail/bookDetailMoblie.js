import { Search } from "@mui/icons-material";
import {
  Link,
  ListItemButton,
  ListItemIcon,
  ListItemText,
} from "@mui/material";
import { BookDetailContainer } from "../../styles/bookDetail";
import SearchIcon from "@mui/icons-material/Search";

import Box from "@mui/material/Box";

export default function BookDetailMoblie({ matches }) {
  return;
}
